# 1.全过程prompt记录

## 1.1规划

- 翻译一下文章，生成脑图

- 根据任务要求，生成二分类检测的复现与改进的软件需求文档

- 现在仅考虑ai帮我从零开始完整完成项目，我的提示词撰写有哪些注意点呢

- 我是在vscode环境下借助copilot完成全过程，以上是我能使用的模型，那么基于此重新修改

- 据此生成一份全流程规划开发手册，包含各阶段的首选模型、兜底模型、提示词、优势、注意点等

- 你可以访问我当前VS Code工作区的文件，我已经把项目的论文PDF上传到工作区了。
帮我做这个航空维护项目的前期调研，输出结构化的总结：
任务定义梳理：
    - 论文中提到的「维护前vs维护后航班二分类任务」的具体定义是什么？输入是什么？标签是什么？
    - 论文Section3.2提到的「19类维护事件的基准子集」具体是什么？这个子集的样本量、特征维度、时间序列长度是多少？
    - 重点确认：这个19类是样本筛选范围，不是分类任务的类别，对不对？绝对不能搞错任务。
论文基线结果提取：
    - 论文中这个二分类任务的 baseline 结果是多少？用的什么模型？指标是什么？
数据集信息梳理：
    - 这个基准子集的下载地址是不是hyang0129/NGAFIDDATASET？具体是哪个文件？
    - 数据集的原始格式是什么？有没有缺失值？类别分布是什么样的？维护前和维护后的样本比例是多少？
领域坑点提醒：
    - 这个任务有什么常见的坑？比如数据泄露之类的，要重点注意什么？
    请你输出结构化的结果，不要太啰嗦。

## 1.2技术实现

- 基于上一步的调研结果，帮我写数据处理的Python代码，你可以直接在我工作区创建文件，不用我手动复制。
要求：
数据自动下载：
   自动从hyang0129/NGAFIDDATASET下载基准子集，带进度条，失败有重试，失败后提示用户手动放的路径
   自动解压，数据存到项目根目录的data/文件夹，自动建文件夹
预处理逻辑：
   加载数据，处理成X是[样本, 时间步, 特征数]，y是二元标签（0=维护后，1=维护前）
   缺失值用线性插值填充，不要丢样本
   预留预处理接口，支持后面交叉验证每折独立做预处理，绝对不能用全数据集的统计量！
路径用pathlib处理，兼容所有系统，不要硬编码绝对路径。
把代码生成到src/data_processor.py，加好注释和docstring。

- 帮我写模型训练和评估的代码，直接生成到工作区的src/train_evaluate.py：
要求：
模型用MiniRocket，时间序列分类的轻量SOTA，CPU就能跑，用来改进论文的基线，不要用简单的逻辑回归。
分层五折交叉验证，固定所有随机种子为42，保证结果可复现：
   每折的流程必须是：
   划分训练/测试集
   仅用训练集做预处理
   训练模型
   用训练集的参数处理测试集，预测评估
   绝对不能有数据泄露！
评估指标：每折计算准确率、加权F1、ROC-AUC，最后输出均值±标准差，加注释说明为什么选这几个指标（不平衡数据下F1和ROC比准确率更可靠）
自动创建results/文件夹，生成混淆矩阵、ROC曲线、五折结果对比图，支持中文显示。
运行的时候输出进度日志，比如「正在运行第1折交叉验证」。
加好注释，保证代码清晰。

- 帮我把前面的模块封装成完整的可运行项目，直接修改我工作区的文件：
写一个main.py入口，用户执行python main.py就能自动跑完所有流程，全程不用手动操作，加异常捕获，出错给清晰提示。
生成requirements.txt，列出所有依赖，兼容Python3.8~3.12，保证pip install -r就能装完。
生成.gitignore，忽略data、results、缓存、虚拟环境这些，不要把数据提交到git。
给我写VS Code里的Git提交步骤，用Source Control面板的操作方式，分5次提交，保证提交历史≥5次，步骤如下：
   第一次：提交初始化的README、.gitignore，提交信息：init: 项目初始化与结构
   第二次：提交数据处理模块，提交信息：feat: 新增数据自动下载与预处理
   第三次：提交训练评估模块，提交信息：feat: 新增模型训练与五折交叉验证
   第四次：提交入口脚本和依赖，提交信息：feat: 新增一键运行入口与依赖配置
   第五次：提交文档，提交信息：docs: 新增项目文档与协作日志
   刚好5次，满足要求。

## 1.3校核优化

- 生成完代码后，检查一下有没有「每折独立预处理」的逻辑，避免 AI 忘了加防泄露的规则

- 测试一下下载逻辑，确保能正常拉取数据集

- 检查所有随机操作是不是都固定了种子，包括交叉验证划分、模型初始化

- 确认交叉验证的流程是不是每折独立做预处理，绝对不能用全数据集的统计量

- 这是我的代码结果，请问符合论文结论吗？对照文章评估结果

- 以上是我的主要代码，能具体说说minimax全局归一化与改进后的minirocket吗，我在个人pc上跑经常出现cpu或内存占用百分百，但也是间歇性的，训练时间还是很长的

- 以上是运行输出结果、管线主函数、我的计划与一阶段调研，评估一下我复现的训练代码

- 评估这份修改意见，综合读取test2文件夹内所有文件给出意见。

- 两个高优先以及一个中高优先都要处理，两个中优先不需要处理，不用补充指标，开始操作吧，注意尽可能少的引入补丁和修改，保证原始框架。从一名reviewer的角度出发审视编写。

- 固定折会不会比随机折减少运算负载

- 删除大数据量和妨碍git上传的冗余文件

- 现在是否还满足一键部署运行

- 清除git中含有冗余大文件的历史版本

- 以上是运行结果，为什么downloader存在问题

- 修复静态报错，新建一个虚拟干净环境进行验证

# 2.学生贡献标注

- 一开始AI建议用逻辑回归 baseline，我改成了MiniRocket模型，因为它是时间序列分类的轻量SOTA，CPU可运行，能比论文基线提升效果

- AI初始的预处理方案想用全数据集的标准化，我发现存在数据泄露风险，要求改成每折独立做预处理，保证实验严谨性

- 我一开始不懂PHM领域的规则，调研后才掌握了时间序列任务防泄露的核心要求。

- 不同类数据的阈值范围差异较大，尤其是在航空数据集中，充满了飞行员操作噪声，且故障多为慢性退化，对此应当分类进行归一化。